import { Action, IPickominoGame, PlayAction } from "../types";

export function pickominoActionsCreator(
  game: IPickominoGame,
  play: React.Dispatch<Action>
): PlayAction[] {
  return []; //todo ARNAUD: à compléter
}
